export const nodeShell = (accent) => ({
    minWidth: 240,
    maxWidth: 280,
    borderRadius: 3,
    border: "1px solid",
    borderColor: "divider",
    borderTop: "3px solid",
    borderTopColor: accent,
    boxShadow: "0 2px 8px rgba(0,0,0,0.08)",
    bgcolor: "background.paper",
});
